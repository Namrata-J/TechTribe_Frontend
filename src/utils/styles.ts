const flexWithCenter = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
};

const flexWithStart = {
  display: "flex",
  alignItems: "center",
  justifyContent: "flex-start",
};

const flexWithSpace = {
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
};

export { flexWithCenter, flexWithStart, flexWithSpace };
